#include<iostream>
#include<string.h>
using namespace std;

int main()
{
	if(strcmp("192.168.43.78.","192.168.43.78.")==0)
	{
		cout<<"ok"<<endl;
	}
	return 0;
}
#if 0
                        if((size!=0)&&(size!=-1))
                        {
                        if(n>=76800)
                        {
                                printf("tuichu\n");
                                break;
                        }
                        if(strcmp("+",buf) == 0)
                        {
                                cout<<"ok"<<endl;
                                break;
                        }
                        fwrite(buf,strlen(buf),1,file_fd);
                        n++;
                        fflush(file_fd);
                        strcpy(buf,str);
                        }
#endif
